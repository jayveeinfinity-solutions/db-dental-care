CANCEL PROCESS (on admin)
    => When cancel, the patient will receive a cancellation message with available
    schedules on the same date that users desired date.

CANCEL PROCESS (on patient)
    => When cancel, the admin see cancel remarks.

Transaction details upon completing the appointment


COMPLETE PROCESS
    => Upload patient file and textarea remarks


Patient records (user)
    => Add page of patient history (transaction)

Scheduled -> Approved


TOTP (Admin side)


Admin (Walk in appointment)
    => Admin will create the account of the user (name, email)
    => Admin direct to transaction (appointment)


Online booking => Appointment => Transaction
Walk in => Appointment => Transaction
Walk in => Transaction

